# Polyworld 运行说明

## 环境要求

- **WSL2 + Ubuntu**（推荐 24.04；原生 Windows 不支持）
- 必须在 **polyworld 项目根目录**（与 `Polyworld` 可执行文件同级）运行
- 依赖：`g++`、`make`、`qtbase5-dev`、`libqt5opengl5-dev`、`libgsl-dev`、`mesa-common-dev`、`python3`

## 首次编译

```bash
cd /mnt/c/Users/glsa-/Documents/GitHub/neat_thinking_unit/Polyworld/polyworld

# 安装依赖（Ubuntu 24.04）
sudo apt-get update
sudo apt-get install -y git g++ make mesa-common-dev libglu1-mesa-dev \
  libgsl-dev qtbase5-dev libqt5opengl5-dev python3

# 配置与编译（configure 已适配 Python3）
sed -i 's/\r$//' configure    # 若在 Windows 盘编辑过，去掉 CRLF
./configure
make app -j$(nproc)
```

> **建议**：将项目复制到 WSL 家目录（如 `~/polyworld`）再编译，可避免 `/mnt/c` 下文件时间戳错乱导致反复重编。

---

## 主程序启动（唯一仿真入口）

```bash
./Polyworld [选项] <worldfile>
```

### 常用选项

| 选项 | 说明 |
|------|------|
| `--ui gui` | 图形界面（**默认**）：多窗口监视器 |
| `--ui term` | 终端模式：无 GUI，可输入 `help` / `end` / `gui` |
| `--参数名 值` | 运行时覆盖 worldfile 参数，如 `--MaxSteps 5000` |

### 快速测试

```bash
# 最快：默认参数，500 步
./Polyworld worldfiles/hello.wf

# 低配短跑：完整路径在 tests/low-spec-pc/ 子目录下（不是 worldfiles 根目录）
./Polyworld worldfiles/tests/low-spec-pc/minitest.wf
```

> 常见错误：写成 `worldfiles/minitest.wf` 或 `mnitest.wf` 会报 `No such file`。

### 终端模式示例

```bash
./Polyworld --ui term worldfiles/hello.wf
```

---

## 研究该项目：最有参考性的入口

> 若你的目标是理解 **「开放生态 + 自然视觉 + 神经演化」**（例如对照你自己的 ocellus 实验、或 Yaeger & Sporns 的神经复杂度路线），请优先看下面几类。**`legacy/` 下无 `@version 2` 的旧 `worldfile_*` 可先不管**——那是论文时代的 v1 配置档案，当前引擎跑不起来，对上手研究帮助不大。

### 按优先级：先跑什么、再读什么

| 优先级 | 类型 | 路径 / 资源 | 为什么值得看 |
|--------|------|-------------|--------------|
| ★★★ | 可运行场景 | `worldfiles/hello.wf` | 确认环境；理解「worldfile = 实验配方」 |
| ★★★ | 可运行场景 | `worldfiles/tests/low-spec-pc/minitest.wf` | **日常主战场**：小世界、301 步、官方回归套件，改参数试错成本低 |
| ★★★ | 可运行场景 | `worldfiles/tests/low-spec-pc/cminitest.wf` | 在 minitest 规模上开启 **神经复杂度记录**，最接近「小规模复杂度实验」 |
| ★★★ | 可运行场景 | `worldfiles/legacy/poison.wf` | **视觉分辨觅食**（绿营养 / 红毒药）；与「学会区分再行动」的研究问题最直接相关 |
| ★★ | 可运行场景 | `worldfiles/social/feed_young.wf` | 社会哺育、能量赠送、动态进食年龄——看 **生态压力如何塑造行为** |
| ★★ | 可运行场景 | `worldfiles/m-neurons/growingBarriers.wf` | 动态障碍 + 迁徙食物；**新版 v2 语法**（`dyn(...)`、钩子）的范例 |
| ★★ | 配置规范 | `etc/worldfile.wfs` | 所有参数的 **schema 与默认值**；自己写实验配方时必读 |
| ★★ | 监视与数据 | `etc/monitors.mfs`、`run/` 输出目录 | GUI 监视器定义；一次运行后看 `run/` 里基因组、大脑、能量、统计 |
| ★★ | 源码（仿真核心） | `src/library/sim/`、`src/app/` | 步进循环、个体生命周期、GUI/终端入口 |
| ★★ | 源码（大脑） | `src/library/brain/`、`src/library/genome/` | 基因组编码、Hebbian 学习、视觉输入、神经网络结构 |
| ★ | 源码（复杂度） | `src/library/complexity/`（及 `RecordComplexity` 相关） | Sporns 类 TSE 复杂度在代码里怎么算、怎么 dump |
| ★ | 分析工具 | `bin/neurons`、`bin/PwMoviePlayer` | 跑完后分析神经元活动、回放 `run/movie.pmv` |
| ★ | 外部论文 | [Yaeger ALife III (1994)](https://shinyverse.org/larryy/Yaeger.ALife3.pdf) | 系统设计动机：代谢、视觉、遗传大脑、开放世界 |
| ★ | 外部论文 | Yaeger & Sporns ALife X (2006) | **神经复杂度趋势**、driven vs passive 实验背景（见 `Polyworld/资料.md`） |
| ★ | 外部站点 | [shinyverse.org/larryy/polyworld.html](https://shinyverse.org/larryy/polyworld.html) | 作者主页：论文、演示视频、安装说明 |
| — | 可忽略（暂） | `legacy/worldfile_driven`、`worldfile_passive`、`worldfile_complexity` 等 | v1 历史档案，**当前不能直接跑**；理解论文背景时可读参数，不必花时间调试 |

### 按研究问题选场景

| 你想搞懂什么 | 建议从哪开始 |
|--------------|--------------|
| 系统怎么跑、GUI 怎么看 | `hello.wf` → `minitest.wf` |
| 视觉能不能驱动觅食策略 | **`poison.wf`**（双色食物，吃错会扣能量） |
| 神经复杂度怎么记录、输出在哪 | **`cminitest.wf`**，跑完查 `run/` 与复杂度相关 dump |
| 社会行为 / 代际供养 | `feed_young.wf`（长跑，20 万步） |
| 环境变化下的导航与集群 | `growingBarriers.wf` |
| 自己改实验参数 | 复制 `minitest.wf` 或 `hello.wf`，对照 `etc/worldfile.wfs` 改 |
| 对照 ocellus「欺骗性适应度」问题 | 重点看 **poison**：错误进食有真实代谢代价；对比你项目里「吃错不影响排名」的设计 |

### 建议阅读 / 运行顺序（研究向）

1. 跑通 `hello.wf`、`minitest.wf`，熟悉 GUI 与 `run/` 目录结构  
2. 跑 `poison.wf`，观察个体是否逐渐避开红色食物（视觉学习的最直观例子）  
3. 跑 `cminitest.wf`，了解复杂度数据如何产生（小规模、耗时短）  
4. 打开 `etc/worldfile.wfs`，对照你改过的 worldfile，理解参数含义  
5. 按需深入 `feed_young.wf` / `growingBarriers.wf` 或读 `src/library/brain/`  
6. **`legacy/` 整目录可后置**：需要复现 2006 年论文级 driven/passive 时再碰

### 与你自己项目的对照（简要）

你在 `总结0529.md` 里分析的 ocellus 问题（通吃、判别不影响繁殖排名），在 Polyworld 里对应的「正面教材」是：

- **开放生态**：没有单一标量目标函数，存活与繁殖由能量、年龄、互动共同决定  
- **视觉是真实渲染**：第一人称 OpenGL，不是二值视网膜  
- **错误有代价**：例如 `poison.wf` 中吃毒药直接伤害代谢  
- **选择压力连续**：觅食、避敌、繁殖、衰老每一步都影响能否留下后代  

因此：**研究 Polyworld 时，优先 `minitest` + `poison` + `cminitest`，比纠结 legacy 旧文件更有参考价值。**

---

## Worldfile 对照（换场景 = 换实验）

> 完整研究入口见上一节 **「研究该项目：最有参考性的入口」**。  
> **worldfile** 是实验配方：世界大小、个体数、大脑、食物、步数等；命令始终是 `./Polyworld <某个.wf>`。

### 一览表（研究向）

| 文件 | 一句话 | 研究参考价值 |
|------|--------|----------------|
| `worldfiles/hello.wf` | 最小冒烟测试 | ★★★ 环境验证 |
| `worldfiles/tests/low-spec-pc/minitest.wf` | 低配短跑（301 步） | ★★★ **日常主场景** |
| `worldfiles/tests/low-spec-pc/cminitest.wf` | 短跑 + 记录复杂度 | ★★★ 复杂度入门 |
| `worldfiles/legacy/poison.wf` | 营养 vs 毒药双色食物 | ★★★ **视觉分辨觅食** |
| `worldfiles/social/feed_young.wf` | 代际哺育 / 社会行为 | ★★ 社会生态 |
| `worldfiles/m-neurons/growingBarriers.wf` | 动态障碍 + 迁徙食物 | ★★ 动态环境、v2 高级语法 |
| `worldfiles/tests/low-spec-pc/microtest.wf` | 1 步极短测试 | ★ 仅确认能启动 |
| `worldfiles/tests/low-spec-pc/medtest.wf` | 更多个体的短跑 | ★ 性能稍高时 |
| `legacy/worldfile_*`（无 `.wf` 后缀） | 论文时代 v1 配置 | — **可先忽略**，见文末附录 |

---

### 各文件详解

#### `worldfiles/hello.wf` — 最小测试

- **做什么**：几乎不覆盖任何参数，只设 `MaxSteps 500`，其余全部用系统默认值（约 300 个个体、有视觉、有食物、标准大脑）。
- **研究目的**：无特定科学问题，仅确认编译、Python 配置解析、GUI 能正常工作。
- **特点**：启动快、步数少，适合第一次运行。
- **命令**：`./Polyworld worldfiles/hello.wf`

---

#### `worldfiles/tests/low-spec-pc/` — 低配机回归测试套件

**目录位置**（注意层级，不在 `worldfiles/` 根下）：

```
worldfiles/tests/low-spec-pc/
├── microtest.wf    # 极小：1 步
├── minitest.wf     # 小：301 步（推荐入门）
├── medtest.wf      # 中：301 步、更多个体
├── cminitest.wf    # 小 + 记录神经复杂度
└── README.txt
```

官方说明：面向低配硬件（如约 1 GHz 的 APU）做回归测试，确认改动未破坏基本仿真。

| 文件 | 步数 | 个体数 | 世界大小 | 额外说明 |
|------|------|--------|----------|----------|
| `microtest.wf` | **1** | 20–25 | 25 | 极快，几乎只验证能启动 |
| `minitest.wf` | **301** | 20–25 | 25 | **日常推荐**，能看一会儿演化 |
| `medtest.wf` | 301 | **50–75** | 25 | 比 minitest 更吃性能 |
| `cminitest.wf` | 301 | 20–25 | 25 | 同 minitest，另开 `RecordComplexity` |

共性：小世界（`WorldSize 25`）、少食物（10–30）、Groups 大脑最多 5 组、开启 `RecordAll` 写运行数据到 `run/`。

**推荐命令**：

```bash
# 日常低配短跑（最常用）
./Polyworld worldfiles/tests/low-spec-pc/minitest.wf

# 只想确认能启动（1 步就结束）
./Polyworld worldfiles/tests/low-spec-pc/microtest.wf

# 稍多个体、略慢
./Polyworld worldfiles/tests/low-spec-pc/medtest.wf

# 需要复杂度记录时
./Polyworld worldfiles/tests/low-spec-pc/cminitest.wf

# 覆盖步数（例如先跑 100 步试试）
./Polyworld --MaxSteps 100 worldfiles/tests/low-spec-pc/minitest.wf
```

**常见错误**：

```text
No such file: worldfiles/minitest.wf     ← 少了 tests/low-spec-pc/
No such file: worldfiles/mnitest.wf      ← 拼写错误，应为 minitest
```

---

#### `worldfiles/social/feed_young.wf` — 社会行为：哺育幼体

- **做什么**：长跑 20 万步；开启 **能量赠送**（`EnableGive`），个体可把能量分给他人；**最小进食年龄**（`MinEatAge`）随种群数量动态变化——种群多时幼体更晚才能自己进食，迫使成体「喂」 young。
- **研究目的**：探索类似 parental care / 社会性供养是否能在演化中自发出现；文件头注释提到曾观察到高接收率、进食年龄演化到 ~280 步。
- **特点**：Groups 大脑架构、记录个体接触（`RecordContacts`）；适合观察长期社会行为，**非常耗时**。
- **命令**：`./Polyworld worldfiles/social/feed_young.wf`

---

#### `worldfiles/m-neurons/growingBarriers.wf` — 动态障碍与迁徙食物

- **做什么**：长跑 20 万步；两道 **屏障** 随种群数量升高而向中间生长、种群崩溃时收缩，改变可活动区域；三个 **食物斑块** 按「令牌环」轮流激活，迫使种群在斑块间迁移。
- **研究目的**：环境非静态时，测试集群导航、迁徙、避障等更复杂行为是否演化出来；作者曾用类似设定观察个体在食物转移时快速奔向下一个斑块。
- **特点**：含动态表达式（`dyn(...)`）和 C++ 钩子（`FoodPatchTokenRing`），是较高级的新版 worldfile 范例。
- **命令**：`./Polyworld worldfiles/m-neurons/growingBarriers.wf`

同目录 `growingBarriers_grayBricks.wf` 在此基础上增加了砖块障碍变体。

---

#### `worldfiles/legacy/poison.wf` — 毒食物 vs 营养食物

- **做什么**：定义两种食物类型——绿色 **营养**（`EnergyPolarity [ 1 ]`）和红色 **毒药**（`EnergyPolarity [ -1 ]`）；在世界不同区域布置多个食物斑块，混合两种类型。
- **研究目的**：个体必须靠 **视觉** 区分颜色，学会只吃绿的、避开红的；观察捕食/觅食策略与能量管理。
- **特点**：已改为标准 **`@version 2`** 格式（3 万步）；旧版无 `@version 2` 时会被自动转换成 `.v2` 文件，其中负数 `EnergyPolarity [ -1 ]` 会转换失败并报 `Unexpected '-'`。
- **命令**：`./Polyworld worldfiles/legacy/poison.wf`

**研究向推荐命令合集**：

```bash
./Polyworld worldfiles/hello.wf
./Polyworld worldfiles/tests/low-spec-pc/minitest.wf
./Polyworld worldfiles/tests/low-spec-pc/cminitest.wf
./Polyworld worldfiles/legacy/poison.wf
```

---

## 附录：`legacy/` 旧版 worldfile（可先忽略）

`worldfiles/legacy/` 里除 **`poison.wf`（已改为 v2，可跑）** 外，大量 `worldfile_driven`、`worldfile_passive`、`worldfile_complexity` 等属于 **2000 年代 v1 格式历史档案**。它们对理解早年论文实验设计有文献价值，但 **当前仓库不能 `./Polyworld` 直接启动**，自动 v1→v2 转换也不完整。

**研究上手阶段建议：整类先跳过**，把时间放在上一节列出的 v2 场景与 `etc/worldfile.wfs` 上。只有当你明确要复现 Yaeger & Sporns (2006) 级 driven/passive 长跑时，再考虑手工迁移或修转换器。

若仍遇到以下报错，属于预期行为，不是你的操作问题：

| 文件 | 典型报错 | 原因 |
|------|----------|------|
| `worldfile_complexity` | `Unexpected '29'` | v1 首行 `29 version` 未正确转换 |
| `worldfile_driven` | `Unexpected '26'` | 同上 |
| `worldfile_passive` | `Unexpected '24'` | 同上 |

<details>
<summary>（可选阅读）driven / passive 实验含义</summary>

- **Driven**：正常死亡压力（饿死、老死、被吃）→ 自然选择在筛个体  
- **Passive**：寿命极长、代谢极低 → 选择极弱，主要靠漂移  
- 两者成对设计，用于论证「神经复杂度上升是否由选择驱动」  

参数细节可读 `docs/NOTES.txt`（若仓库中有）或直接打开旧文件文本，**不必运行**。

</details>

---

## GUI 界面说明

- **运行** 菜单：暂停/继续、单步、指定时间步结束、立即结束
- **视图** 菜单：开关各监视器窗口（出生率、适应度、主场景、大脑监视器等）
- 界面已中文化；场景标题见 `etc/monitors.mfs`（主场景、俯视图、单视角）

---

## 其他可执行程序（非主仿真）

编译后位于 `bin/`，需单独调用：

| 程序 | 用途 |
|------|------|
| `bin/PwMoviePlayer` | 播放 `run/movie.pmv` 录像 |
| `bin/passive` | driven/passive 批量对照 |
| `bin/nullevo` | 零演化对照 |
| `bin/neurons` | 神经元分析 |

---

## 常见问题

### 1. `PANIC! [proplib/interpreter.cc:170]`

系统没有 `python` 命令。任选其一：

```bash
sudo apt-get install -y python-is-python3
```

或：

```bash
mkdir -p ~/bin && ln -sf /usr/bin/python3 ~/bin/python
echo 'export PATH=~/bin:$PATH' >> ~/.bashrc && source ~/.bashrc
```

### 2. `Required statement failed: success == 'S' || success == 'F'`

配置解析时 Python 子进程协议失败。常见原因：

- 未安装 `python3` / 未设置 `python` 别名
- **界面中文化后** 配置含中文，需使用已修复的 `src/library/proplib/interpreter.py`（按 UTF-8 字节长度通信）

确认后重新运行即可。

### 3. `/usr/bin/env: 'python3\r': No such file or directory`

`configure` 文件为 Windows 换行符（CRLF）：

```bash
sed -i 's/\r$//' configure
```

### 4. `libqt4-dev` / `python2.7` 找不到

Ubuntu 24.04 已移除 Qt4 和 Python2。本项目已改用 **Qt5 + Python3** 编译。

### 5. 每次启动都重新编译 `run/.cppprops`

项目在 Windows 盘（`/mnt/c/...`）时易出现时间戳问题。将项目移到 `~/polyworld` 可缓解。

### 6. GUI 黑屏 / OpenGL 警告

需 WSLg（Win11）或 X Server；`QOpenGLContext::swapBuffers` 警告在 WSL 下较常见，一般不影响运行。

### 7. `No such file: worldfiles/minitest.wf`

`minitest.wf` 不在 `worldfiles/` 根目录，完整路径为：

```bash
./Polyworld worldfiles/tests/low-spec-pc/minitest.wf
```

注意拼写是 **mini**test，不是 `mnitest`。

### 8. `poison.wf` 报 `Unexpected '-'` 或 `poison.wf.v2:18`

旧版 `poison.wf` 缺少 `@version 2`，Polyworld 会自动生成 `poison.wf.v2`，但转换器会把 `EnergyPolarity [ -1 ]` 弄坏。

**处理**：使用仓库里已修复的 `poison.wf`（含 `@version 2`），并删除残留的 `poison.wf.v2`：

```bash
rm -f worldfiles/legacy/poison.wf.v2
./Polyworld worldfiles/legacy/poison.wf
```

### 9. `worldfile_complexity` / `worldfile_driven` / `worldfile_passive` 报 `Unexpected '29'` 等

属于 **`legacy/` 旧 v1 配置**，研究阶段可先忽略。详见文末附录「`legacy/` 旧版 worldfile」。

---

## 输出目录

每次运行会在当前目录创建 `run/`，旧结果自动改名为 `run_<时间戳>/`。内含统计、录像、转换后的 `converted.wf` 等。
