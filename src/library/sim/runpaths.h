#pragma once

// All simulation output (logs, genomes, movies, cppprops build) lives under runs/run/.
// Previous runs are archived as runs/run_<unix_timestamp>/.

#define PW_RUNS_DIR "runs"
#define PW_RUN_DIR  "runs/run"
